import React from 'react';
import {View} from 'react-native';
import {globalStyles} from 'globalstyles';

export default function ListContainer() {
  return <View style={globalStyles.containers} />;
}
